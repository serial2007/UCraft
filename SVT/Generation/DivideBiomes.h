#pragma once
#include "../Generation/Entry.h"

namespace UBiomes
{
	extern void DivideBiomes(Generation::WorldUnit* unit);
	extern void TreeBiomeExpand(Generation::WorldUnit* unit, int x, int y, int deg);
}